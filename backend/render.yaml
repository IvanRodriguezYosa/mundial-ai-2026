services:
  - type: web
    name: mundial-ai-2026-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $port
    envVars:
  - key: ANTHROPIC_API_KEY
    sync : false